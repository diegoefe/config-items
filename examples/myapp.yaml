name: ThisIsMyName
network:
  proxy:
    url: myproxy
    port: 8080
    password: pepito
skip_ssl_validation: true
logging:
  file_name: mycustom.log
  level: trace # info/error/debug/trace, defaults to info